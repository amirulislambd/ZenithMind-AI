import ChatPage from '../../../../components/chat/page';

export const metadata = {
  title: 'Dashboard — Chat | ZenithMind AI',
  description: 'Chat with the ZenithMind executive burnout coach',
};

export default function DashboardChatRoute() {
  return <ChatPage />;
}
